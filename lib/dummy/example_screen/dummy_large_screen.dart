import 'package:flutter/material.dart';

class DummyLargeScreen extends StatelessWidget {
  const DummyLargeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

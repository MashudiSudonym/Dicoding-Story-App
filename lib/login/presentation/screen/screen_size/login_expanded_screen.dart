import 'package:flutter/material.dart';

class LoginExpandedScreen extends StatefulWidget {
  const LoginExpandedScreen({Key? key}) : super(key: key);

  @override
  State<LoginExpandedScreen> createState() => _LoginExpandedScreenState();
}

class _LoginExpandedScreenState extends State<LoginExpandedScreen> {
  @override
  Widget build(BuildContext context) {
    return const Text('small screen');
  }
}
